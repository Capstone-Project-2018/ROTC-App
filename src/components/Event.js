import React from "react";

export default class Event extends React.Component { 
    render() {
        const {
            events
        } = this.props;
        return(
            <div>

            </div>
        );
    }
}